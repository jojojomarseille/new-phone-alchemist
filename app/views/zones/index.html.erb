<h1>Zones#index</h1>

<div class="cards">
    <%= paginate @zones %>
    <% @zones.each do |zone| %>
    <div class="card" style="width: 18rem;">
      <img src="..." class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title"><a href="/zones/<%= zone.id %>"><%= zone.name%></a></h5>
        <p class="card-text"><%= zone.description%></p>

        <a href="/zones/<%= zone.id %>" class="btn btn-primary">voire</a>
      </div>
    </div>
    <% end %>
  </div>
</div>


<p> <a href="/zones/new" class="btn btn-primary"> Creer une nouvelle zone</a>
